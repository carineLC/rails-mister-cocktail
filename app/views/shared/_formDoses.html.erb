<%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.association :ingredient, collection: Ingredient.order(:name), input_html: { class: "js-example-basic-single" } %>
    <%= f.input :description %>
    <%= f.submit "Add new ingredient", class: "btn btn-primary" %>
<% end %>
